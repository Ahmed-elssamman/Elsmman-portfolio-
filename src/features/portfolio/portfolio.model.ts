export interface NavigationItem {
  id: string;
  label: string;
}

export interface Expertise {
  number: string;
  title: string;
  description: string;
  tools: string[];
}

export interface ProjectPresentation {
  image: string;
  alt: string;
  theme: string;
  label: string;
}
