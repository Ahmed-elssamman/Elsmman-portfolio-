// Deprecated — content now lives in /data/profile.json and is loaded server-side
// via src/lib/data.ts. Edit it through /admin/27348 or by editing the JSON file.
export type { Profile } from "@/lib/data-schemas";
